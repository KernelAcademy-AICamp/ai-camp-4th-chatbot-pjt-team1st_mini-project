# 📱 박물관 챗봇 앱 컴포넌트 구조

> `app_figma.py` 기준 (Streamlit + Figma 디자인)

---

## 1️⃣ 레이아웃 컴포넌트 (고정 UI)

| 컴포넌트 | 클래스명 | 설명 | 위치 |
|----------|----------|------|------|
| **헤더** | `.figma-header` | "ChatBot" 타이틀 + 검색/메뉴 아이콘 | `position: fixed; top: 0` (높이: 103px) |
| **서브헤더** | `.figma-subheader` | 새로고침 아이콘 + "국립중앙박물관 스마트전시관" | `position: fixed; top: 103px` (높이: 47px) |
| **채팅 영역** | `.figma-chat-container` | 메시지 표시 영역 | `margin-top: 150px; margin-bottom: 164px` |
| **하단 네비게이션** | `.figma-bottom-nav` | 5개 탭 (홈, 전시, AR 전시, 대표 누리집, 챗봇) | `position: fixed; bottom: 0` (높이: 104px) |
| **입력 필드** | `.stChatInput` | 메시지 입력창 | `position: fixed; bottom: 104px` |

### 입력 필드 (Chat Input) 상세 구조

```
┌─────────────────────────────────────────────────────────────┐
│  wag (배경 블러 레이어)                                       │
│  ├── position: fixed, bottom: 104px                         │
│  ├── width: 393px, height: 65px                            │
│  ├── backdrop-filter: blur(2px)                            │
│  └── background: gradient (transparent → rgba(255,255,255,0.3))
├─────────────────────────────────────────────────────────────┤
│  .stChatInput (Input Container)                             │
│  ├── padding: 10px 15px 15px 15px                          │
│  ├── backdrop-filter: blur(2px)                            │
│  │                                                          │
│  │  ┌─ Input Field ──────────────────────────────────────┐  │
│  │  │  ├── background: #f1f2f6                           │  │
│  │  │  ├── border: 1px solid #eeeeee                     │  │
│  │  │  ├── border-radius: 100px                          │  │
│  │  │  ├── padding: 10px 45px 10px 17px                  │  │
│  │  │  │                                                 │  │
│  │  │  │  ┌─────────────────────────┐   ┌────────────┐   │  │
│  │  │  │  │ Placeholder             │   │ Send Icon  │   │  │
│  │  │  │  │ "메시지를 입력하세요..." │   │ 20x20px    │   │  │
│  │  │  │  │ color: #7e7f8a          │   │ icon_send  │   │  │
│  │  │  │  │ font: Pretendard 16px   │   │   .png     │   │  │
│  │  │  │  └─────────────────────────┘   └────────────┘   │  │
│  │  │  │                                                 │  │
│  │  └────────────────────────────────────────────────────┘  │
│  │                                                          │
└──┴──────────────────────────────────────────────────────────┘
```

#### 입력 필드 스펙

| 요소 | 속성 | 값 |
|------|------|-----|
| **wag** | width | 393px |
| | height | 65px |
| | backdrop-filter | blur(2px) |
| | background | linear-gradient(transparent → rgba(255,255,255,0.3)) |
| **Input Container** | padding | 10px 15px 15px 15px |
| | backdrop-filter | blur(2px) |
| **Input Field** | background | `#f1f2f6` |
| | border | 1px solid `#eeeeee` |
| | border-radius | 100px |
| | padding | 10px 45px 10px 17px |
| **Placeholder** | font | Pretendard Regular 16px |
| | color | `#7e7f8a` |
| | line-height | 15px |
| **Send Icon** | size | 20px × 20px |
| | image | `icon_send.png` |

### 레이아웃 다이어그램

```
┌─────────────────────────────────┐
│         figma-header            │  ← 103px (고정)
│         "ChatBot"               │
├─────────────────────────────────┤
│        figma-subheader          │  ← 47px (고정)
│   "국립중앙박물관 스마트전시관"   │
├─────────────────────────────────┤
│                                 │
│      figma-chat-container       │  ← 스크롤 영역
│         (채팅 메시지들)          │
│                                 │
├─────────────────────────────────┤
│         stChatInput             │  ← 60px (고정)
│      "메시지를 입력하세요..."     │
├─────────────────────────────────┤
│       figma-bottom-nav          │  ← 104px (고정)
│   홈 | 전시 | AR | 누리집 | 챗봇  │
└─────────────────────────────────┘
```

---

## 2️⃣ 채팅 메시지 컴포넌트

| 컴포넌트 | 클래스명 | 설명 |
|----------|----------|------|
| **세션 래퍼** | `.chat-session-wrapper` | 봇+사용자 한 세트 (간격: 30px) |
| **채팅 턴** | `.chat-turn` | 개별 메시지 래퍼 (간격: 15px) |
| **봇 메시지 컨테이너** | `.bot-message-container` | 봇 메시지 전체 (w: 363px, 간격: 15px) |
| **봇 콘텐츠 래퍼** | `.bot-content-wrapper` | header + bubble(s) 묶음 (간격: 8px) |
| **봇 헤더** | `.bot-header` | 아바타 + 봇이름 + 타임스탬프 |
| **봇 아바타** | `.bot-avatar` | 28px 원형 프로필 이미지 |
| **봇 정보** | `.bot-info` | 봇 이름(text) + 타임스탬프(datetime) |
| **봇 버블** | `.bot-bubble` | 메시지 버블 (배경: #e7eef7) |
| **사용자 메시지 컨테이너** | `.user-message-container` | 사용자 메시지 전체 (우측 정렬) |
| **사용자 버블** | `.user-bubble` | 메시지 버블 (배경: #246beb) |

### 전체 구조 다이어그램

```
.figma-chat-container
    │
    ├── .chat-session-wrapper  ────────────── 세션 1 (봇+사용자)
    │       │
    │       ├── .chat-turn
    │       │       └── .bot-message-container
    │       │               └── .bot-content-wrapper
    │       │                       ├── .bot-header
    │       │                       │       ├── .bot-avatar (img)
    │       │                       │       └── .bot-info
    │       │                       │               ├── 철수 (text)
    │       │                       │               └── 14:35 (datetime)
    │       │                       ├── .bot-bubble
    │       │                       └── .bot-bubble (여러 개 가능)
    │       │
    │       └── .chat-turn
    │               └── .user-message-container
    │                       ├── timestamp
    │                       └── .user-bubble
    │
    │                 ↕ 30px
    │
    ├── .chat-session-wrapper  ────────────── 세션 2
    │       ├── .chat-turn (봇)
    │       └── .chat-turn (사용자)
    │
    └── ...
```

### 간격 정리

| 위치 | 간격 |
|------|------|
| `.chat-session-wrapper` 간 | **30px** |
| `.chat-turn` 간 (같은 세션 내 봇↔사용자) | **15px** |
| `.bot-message-container` 내 (wrapper ↔ 다른 컴포넌트) | **15px** |
| `.bot-content-wrapper` 내 (header ↔ bubble, bubble ↔ bubble) | **8px** |

### 봇 메시지 상세 구조

```
┌─ .bot-message-container (w: 363px, gap: 15px) ───────────┐
│                                                          │
│  ┌─ .bot-content-wrapper (gap: 8px) ──────────────────┐  │
│  │                                                    │  │
│  │  ┌─ .bot-header ────────────────────────────────┐  │  │
│  │  │  ┌──────────┐  ┌─────────────────────────┐   │  │  │
│  │  │  │  avatar  │  │ .bot-info               │   │  │  │
│  │  │  │  (28px)  │  │  ┌─────────┐ ┌───────┐  │   │  │  │
│  │  │  │   img    │  │  │ 철수    │ │ 14:35 │  │   │  │  │
│  │  │  └──────────┘  │  │ (text)  │ │(time) │  │   │  │  │
│  │  │                │  └─────────┘ └───────┘  │   │  │  │
│  │  │                └─────────────────────────┘   │  │  │
│  │  └──────────────────────────────────────────────┘  │  │
│  │                         ↕ 8px                      │  │
│  │  ┌─ .bot-bubble ────────────────────────────────┐  │  │
│  │  │  메시지 내용 1                                │  │  │
│  │  │  (배경: #e7eef7, border-radius: 0 10 10 10)  │  │  │
│  │  └──────────────────────────────────────────────┘  │  │
│  │                         ↕ 8px                      │  │
│  │  ┌─ .bot-bubble ────────────────────────────────┐  │  │
│  │  │  메시지 내용 2 (여러 개일 경우)                 │  │  │
│  │  └──────────────────────────────────────────────┘  │  │
│  │                                                    │  │
│  └────────────────────────────────────────────────────┘  │
│                            ↕ 15px                        │
│  ┌─ .button-group (옵션) ──────────────────────────────┐  │
│  │  ┌──────────────┐  ┌──────────────┐                │  │
│  │  │   버튼 1     │  │   버튼 2     │                │  │
│  │  └──────────────┘  └──────────────┘                │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### 사용자 메시지 구조

```
┌─ .user-message-container ─────────────────┐
│                          14:35  ┌───────┐ │
│                                 │ 메시지 │ │
│                                 │#246beb│ │
│                                 └───────┘ │
└───────────────────────────────────────────┘
```

---

## 3️⃣ 대화 플로우 (STEPS)

```python
STEPS = {
    "GREETING": "greeting",           # 인사
    "USER_TYPE": "user_type",         # 연령 선택
    "TOUR_CHECK": "tour_check",       # 전시투어 확인
    "ARTIFACT_LIST": "artifact_list", # (미사용)
    "ARTIFACT_SELECT": "artifact_select", # 유물 선택
    "QUIZ_READY": "quiz_ready",       # 퀴즈 준비
    "QUIZ_QUESTION": "quiz_question", # 퀴즈 문제
    "QUIZ_FEEDBACK": "quiz_feedback", # 퀴즈 피드백
    "QUIZ_RESULT": "quiz_result",     # 결과 표시
    "END": "end"                      # 종료
}
```

### 플로우 다이어그램

```
GREETING
    │
    ▼
USER_TYPE ─────────────────┐
    │                      │
    ▼                      │
TOUR_CHECK                 │
    │                      │
    ├─ Yes ─┐              │
    │       ▼              │
    │   ARTIFACT_SELECT    │
    │       │              │
    │       ▼              │
    │   QUIZ_READY         │
    │       │              │
    │       ▼              │
    │   QUIZ_QUESTION ◄────┤
    │       │              │
    │       ▼              │
    │   QUIZ_FEEDBACK ─────┤
    │       │              │
    │       ▼              │
    │   QUIZ_RESULT        │
    │       │              │
    ├─ No ──┼──────────────┘
    │       │
    ▼       ▼
   END ◄────┘
```

---

## 4️⃣ 핸들러 함수 (비즈니스 로직)

| 함수 | 역할 | 연결된 STEP |
|------|------|-------------|
| `handle_greeting()` | 첫 인사 메시지 출력 | GREETING → USER_TYPE |
| `handle_user_type_selection()` | 연령 선택 처리 | USER_TYPE → TOUR_CHECK |
| `handle_tour_check()` | 전시투어 유무 확인 | TOUR_CHECK → ARTIFACT_SELECT / END |
| `handle_artifact_selection()` | 유물 선택 처리 | ARTIFACT_SELECT → QUIZ_READY |
| `handle_quiz_start()` | 퀴즈 시작 | QUIZ_READY → QUIZ_QUESTION |
| `show_next_question()` | 다음 문제 출제 | QUIZ_QUESTION |
| `handle_quiz_answer()` | 퀴즈 답변 처리 | QUIZ_QUESTION → QUIZ_FEEDBACK |
| `show_quiz_result()` | 결과 표시 | → QUIZ_RESULT |
| `handle_review_wrong()` | 오답 복습 | QUIZ_RESULT |
| `handle_end()` | 대화 종료 | → END |
| `handle_free_input()` | 자유 텍스트 입력 처리 | 모든 단계 |

---

## 5️⃣ 인터랙티브 컴포넌트 (Streamlit 위젯)

| 단계 | 컴포넌트 | 코드 |
|------|----------|------|
| **USER_TYPE** | 5개 버튼 (어린이~성인) | `st.button(user_type)` |
| **TOUR_CHECK** | 2개 버튼 (응/아니) | `st.button(yes_text)`, `st.button(no_text)` |
| **ARTIFACT_SELECT** | 체크박스 + 완료 버튼 | `st.checkbox()`, `st.button("선택 완료!")` |
| **QUIZ_READY** | 시작 버튼 | `st.button("준비 완료! 🚀")` |
| **QUIZ_QUESTION** | 5개 선택지 버튼 | `st.button(f"{'①②③④⑤'[i]} {option}")` |
| **QUIZ_FEEDBACK** | 다음 버튼 | `st.button("다음 문제! ➡️")` |
| **QUIZ_RESULT** | 3개 버튼 | 오답복습 / 다시풀기 / 끝내기 |
| **END** | 재시작 버튼 | `st.button("처음부터 다시 시작 🔄")` |
| **공통** | 채팅 입력 | `st.chat_input("메시지를 입력하세요...")` |

---

## 6️⃣ 메시지 타입 시스템

| 타입 | 설명 | 사용처 |
|------|------|--------|
| **Type A** | 일반 메시지 (버튼 없음) | 기본 대화, 설명 |
| **Type B** | 단일 버튼 포함 | (현재 미사용) |
| **Type C** | 다중 버튼 포함 | 연령 선택, 투어 확인 |

### 메시지 데이터 구조

```python
# 봇 메시지
{
    "role": "assistant",
    "content": "메시지 내용",
    "timestamp": "14:35",
    "sender": "국립중앙박물관",
    "type": "A" | "B" | "C",
    "button": {...},      # Type B
    "buttons": [...]      # Type C
}

# 사용자 메시지
{
    "role": "user",
    "content": "메시지 내용",
    "timestamp": "14:35",
    "type": "A"
}
```

---

## 7️⃣ 세션 상태 (st.session_state)

```python
st.session_state = {
    # 대화 관련
    "messages": [],              # 대화 히스토리
    "current_step": "greeting",  # 현재 단계
    
    # 사용자 정보
    "user_type": None,           # 선택된 연령 (어린이/초등학생/중학생/고등학생/성인)
    "tour_exists": True,         # 전시투어 존재 여부
    
    # 유물 관련
    "user_artifacts": [...],     # 전체 유물 목록
    "selected_artifacts": [],    # 퀴즈용 선택된 유물
    
    # 퀴즈 관련
    "quiz_progress": {
        "current_index": 0,      # 현재 문제 번호
        "total_questions": 0,    # 총 문제 수
        "correct_count": 0,      # 맞은 개수
        "wrong_answers": [],     # 틀린 문제 목록
        "quizzes": []            # 퀴즈 데이터 배열
    },
    "current_quiz": None,        # 현재 퀴즈 데이터
    
    # 기타
    "language": "ko",            # 언어 설정
    "llm_service": LLMService()  # LLM 서비스 인스턴스
}
```

---

## 8️⃣ 외부 의존성

| 파일 | 내용 |
|------|------|
| `config/prompts.py` | 메시지 템플릿, UI 라벨 |
| `config/settings.py` | 앱 설정, 언어 설정 |
| `data/artifacts.py` | 유물 데이터 (ARTIFACTS 딕셔너리) |
| `services/llm_service.py` | LLM 서비스 클래스 |
| `components/chat_bubbles.py` | 채팅 버블 렌더링 함수 (import만 되어 있고 미사용) |

### Import 구조

```python
# 설정
from config.prompts import WELCOME_MESSAGES, MESSAGES, UI_LABELS
from config.settings import APP_CONFIG, SUPPORTED_LANGUAGES, DEFAULT_LANGUAGE

# 데이터
from data.artifacts import ARTIFACTS, find_artifact, get_artifact_list

# 서비스
from services.llm_service import LLMService

# 컴포넌트 (미사용)
from components.chat_bubbles import (
    render_type_a_bot,
    render_type_a_user,
    render_type_b_bot,
    render_type_c_bot
)
```

---

## 9️⃣ 컴포넌트 계층 구조

```
🖥️ app_figma.py
│
├── 📦 레이아웃 (HTML/CSS)
│   ├── figma-header (고정, z-index: 1000)
│   ├── figma-subheader (고정, z-index: 999)
│   ├── figma-chat-container (스크롤)
│   ├── stChatInput (고정, z-index: 998)
│   └── figma-bottom-nav (고정, z-index: 1000)
│
├── 💬 채팅 메시지 (HTML)
│   ├── bot-message-container
│   │   ├── bot-avatar (28px)
│   │   ├── bot-info (name + timestamp)
│   │   └── bot-bubble (#e7eef7)
│   └── user-message-container
│       ├── user-timestamp
│       └── user-bubble (#246beb)
│
├── 🎮 인터랙션 (Streamlit 위젯)
│   ├── st.button (다양한 단계)
│   ├── st.checkbox (유물 선택)
│   ├── st.columns (버튼 그룹)
│   └── st.chat_input (자유 입력)
│
├── 🧠 비즈니스 로직 (Python 함수)
│   ├── handle_greeting()
│   ├── handle_user_type_selection()
│   ├── handle_tour_check()
│   ├── handle_artifact_selection()
│   ├── handle_quiz_start()
│   ├── show_next_question()
│   ├── handle_quiz_answer()
│   ├── show_quiz_result()
│   ├── handle_review_wrong()
│   ├── handle_end()
│   └── handle_free_input()
│
└── 🎨 유틸리티 함수
    ├── add_bot_message()
    ├── add_user_message()
    ├── get_tone_text()
    ├── generate_quiz()
    ├── render_messages()
    └── get_last_message_buttons()
```

---

## 🎨 디자인 토큰

| 요소 | 값 |
|------|-----|
| **헤더 배경** | `#07364a` |
| **서브 헤더 배경** | `#e7eef7` |
| **봇 메시지 버블** | `#e7eef7` |
| **사용자 메시지 버블** | `#246beb` |
| **텍스트 (기본)** | `#333333` |
| **텍스트 (보조)** | `#7a7a7a` |
| **Placeholder 텍스트** | `#7e7f8a` |
| **폰트** | Pretendard |
| **버튼 테두리** | `#cccccc` |
| **버튼 border-radius** | `1000px` (pill) |
| **메시지 border-radius** | `10px` |
| **입력 필드 배경** | `#f1f2f6` |
| **입력 필드 테두리** | `#eeeeee` |
| **입력 필드 border-radius** | `100px` |

---

## 📱 반응형 설계

- **최대 너비**: 393px (iPhone 14 기준)
- **채팅 컨텐츠 최대 너비**: 363px
- **봇 버블 최대 너비**: 343px
- **사용자 버블 최대 너비**: 307px

---

## 🔄 상태 전이 다이어그램

```
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│  [시작] ──► GREETING ──► USER_TYPE ──► TOUR_CHECK            │
│                                            │                 │
│                              ┌─────────────┴─────────────┐   │
│                              │                           │   │
│                              ▼                           ▼   │
│                      ARTIFACT_SELECT                    END  │
│                              │                           ▲   │
│                              ▼                           │   │
│                         QUIZ_READY                       │   │
│                              │                           │   │
│                              ▼                           │   │
│                    ┌─► QUIZ_QUESTION ◄─┐                 │   │
│                    │         │         │                 │   │
│                    │         ▼         │                 │   │
│                    │   QUIZ_FEEDBACK ──┘                 │   │
│                    │         │                           │   │
│                    │         ▼                           │   │
│                    │    QUIZ_RESULT ─────────────────────┤   │
│                    │         │                           │   │
│                    └─────────┘ (다시 풀기)                │   │
│                                                          │   │
└──────────────────────────────────────────────────────────┘   │
```

---

*최종 업데이트: 2026-01-13*
